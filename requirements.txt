# Libraries go here as we add them, one at a time, for a specific reason.
# Install with: pip install -r requirements.txt

ultralytics       # loads/runs the trained YOLO detector models (.pt files)
opencv-python-headless  # video/image I/O and drawing; headless since this runs on a drone/server, no display
PyYAML            # reads configs/default.yaml

